# Sales Report Merger

## Overview
A Python utility designed to merge and process sales report data.

## Project Structure
- `src/`: Core source code
- `scripts/`: Execution scripts
- `data/`: Sales report datasets (ignored by Git)
- `tests/`: Automated tests

## Setup
*Instructions will be added as features are developed.*